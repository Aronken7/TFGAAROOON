package com.example.tfg_aaron.ui.utils

import androidx.compose.runtime.compositionLocalOf

/** true when the screen width >= 600dp (tablets, large phones in landscape) */
val LocalIsTablet = compositionLocalOf { false }
